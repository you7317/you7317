# Agent Instructions

이 저장소에서 작업하는 모든 AI 코딩 에이전트를 위한 공통 규칙. 아키텍처/데이터 모델 설명은 `CLAUDE.md`를 참고할 것 — 이 문서는 "어떻게 작업할지"에 집중한다.

## 하지 말아야 할 것

- **빌드 도구 추가 금지.** 이 프로젝트는 의도적으로 npm/번들러/프레임워크가 없다. React, Vite, Webpack, TypeScript 등을 끌어오지 말 것. 필요하면 사용자에게 먼저 이유를 확인한다.
- **`window.prompt`/`window.confirm`/`window.alert` 사용 금지.** Claude Artifact iframe 환경에서 실측으로 불안정함이 확인됐다. `app.js`의 `confirmDialog()`/`promptDialog()` 패턴을 따를 것.
- **`innerHTML`에 미이스케이프 문자열 삽입 금지.** 사용자 입력이 섞이는 모든 템플릿 문자열은 `esc()`를 거친다.
- **탐지 로직(`detectors.js`)과 화면 렌더링(`app.js`)의 판단 기준을 분리해서 재구현하지 말 것.** 화면은 항상 `detectors.js`의 함수를 그대로 호출해야 한다 — 로직을 화면에 복붙하면 테스트와 실제 동작이 어긋난다.
- **`.env`나 비밀값을 커밋하지 말 것.** 현재 이 프로젝트엔 서버/API 키가 없지만, 추가되면 반드시 `.gitignore`에 반영한다.

## 코드 스타일

- 바닐라 JS, `"use strict"`, IIFE로 모듈 스코프 격리(`(function () { ... })()`).
- 주석은 "왜"가 비직관적일 때만 짧게. 코드가 스스로 설명하지 못하는 제약/트릭에만 남긴다.
- 한국어 UI 문자열은 그대로 두되, 코드 식별자(함수명·변수명)는 영어로 유지해 일관성을 지킨다.
- 새 리스크 탐지 규칙을 추가할 때는: ① `detectors.js`에 순수 함수 추가 → ② `tests.js`에 양성·음성 케이스 추가 → ③ 필요한 화면에서 해당 함수 호출. 이 순서를 지킨다.

## 커밋 전 체크리스트

1. `index.html`을 정적 서버로 열어 대시보드 4개 리스크 카드가 정상 표시되는지 확인.
2. `#/tests` 화면에서 테스트가 전부 통과하는지 확인.
3. 375px 폭(모바일)에서 레이아웃이 깨지지 않는지 확인.
4. 새로 추가한 사용자 입력 필드가 `esc()` 처리되는지 확인.

## 배포에 대한 안내

이 저장소는 소스 보관용이다. 실제 서비스 반영은 Claude Artifact 퍼블리시를 통해서만 이루어지며, 이 저장소에 push하는 것만으로는 라이브 앱에 반영되지 않는다. 배포가 필요하면 사용자에게 안내할 것.
